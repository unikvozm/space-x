import { test } from 'Pages/test/constants'
import { history } from 'Pages/history/constants'
import { launches } from 'Pages/launches/constants'

export const ActionTypes = {
	...test,
	...history,
	...launches,
}
